import 'package:flutter/material.dart';

// TODO: implement
class PrimaryButton extends StatelessWidget {
  const PrimaryButton({super.key});

  @override
  Widget build(BuildContext context) {
    return TextButton(onPressed: () {}, child: const Text('todo:'));
  }
}
